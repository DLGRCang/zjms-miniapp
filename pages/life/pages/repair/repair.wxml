<cu-custom bgColor="bgColor" isBack="{{true}}">
  <view slot="content" class="text-white text-xl text-black text-bold">报修服务</view>
</cu-custom>
<image mode="widthFix" src="{{imgUrl}}/image/baoxiu.png" style="width:100%;height:396rpx;"></image>
<tab bindmyevent="selectTab" tvColor="color" tabName="{{tabName}}"></tab>
<view class="margin-lr-lg padding-tb-lg">
  <dot-title title="上报历史" color="color" isMore="true" sign="{{infotypeid}}"></dot-title>
  <view  wx:if="{{tabId==0}}">
    <view wx:for="{{dataList}}" wx:key="item">
      <item-image-text imgUrl="{{item.info_photos}}" obj="{{item}}" itemTitle="{{item.info_content}}" isImg="true" isDate="true" date="{{item.publishdate}}">
      </item-image-text>
    </view>
  </view>
  <view wx:if="{{tabId==1}}">
    <view wx:for="{{dataList}}" wx:key="item">
      <item-image-text imgUrl="{{item.info_photos}}" obj="{{item}}" itemTitle="{{item.info_content}}" isImg="true" isDate="true" date="{{item.publishdate}}">
      </item-image-text>
    </view>
  </view>
  <view wx:if="{{tabId==2}}">
    <view wx:for="{{dataList}}" wx:key="item">
      <item-image-text imgUrl="{{item.info_photos}}" obj="{{item}}" itemTitle="{{item.info_content}}" isImg="true" isDate="true" date="{{item.publishdate}}">
      </item-image-text>
    </view>
  </view>
  <view wx:if="{{tabId==3}}">
    <view wx:for="{{dataList}}" wx:key="item">
      <item-image-text imgUrl="{{item.info_photos}}" obj="{{item}}" itemTitle="{{item.info_content}}" isImg="true" isDate="true" date="{{item.publishdate}}">
      </item-image-text>
    </view>
  </view>
  <view wx:if="{{tabId==4}}">
    <view wx:for="{{dataList}}" wx:key="item">
      <item-image-text imgUrl="{{item.info_photos}}" obj="{{item}}" itemTitle="{{item.info_content}}" isImg="true" isDate="true" date="{{item.publishdate}}">
      </item-image-text>
    </view>
  </view>
  



</view>