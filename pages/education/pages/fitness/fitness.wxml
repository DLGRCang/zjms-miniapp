<cu-custom bgColor="bgColor" isBack="{{true}}">
  <view slot="content" class="text-white text-xl text-black text-bold">文体健身</view>
</cu-custom>
<image mode="widthFix" src="{{imgUrl}}/image/wentijianshen.png" style="width:100%;height:396rpx;"></image>
<view class="margin-lr-lg padding-tb-lg">
  <dot-title title="体育场馆" color="color" isMore="true" sign="{{infotypeid}}" type="3"></dot-title>
  <item-image-text wx:for="{{dataList}}" obj="{{item}}" imgUrl="{{item.info_photos}}" itemTitle="{{item.info_content}}"
    itemInfo="{{item.info_summary}}" isImg="true" isDate="true" date="{{item.publishdate}}" isLine="true">
  </item-image-text>
  <dot-title title="赛事" color="color" isMore="true" sign="{{infotypeid1}}" type="3"></dot-title>
  <item-image-text wx:for="{{dataList1}}" obj="{{item}}" imgUrl="{{item.info_photos}}" itemTitle="{{item.info_content}}"
    itemInfo="{{item.info_summary}}" isImg="true" isDate="true" date="{{item.publishdate}}" isLine="true">
  </item-image-text>
  <!-- <view class="flex justify-between">
    <view wx:for="{{datalist1}}" wx:key="item">
      <view>
        <image src="{{baseImgUrl}}{{item_photos}}" style="height:276rpx;width:200rpx;"></image>
      </view>
      <view class="margin-tb-xs">{{item.info_content}}</view>
      <view class="text-sm text-grey">浏览 203 次</view>
    </view>
  </view> -->
  <dot-title title="演出"  color="color" isMore="true" sign="{{infotypeid2}}" type="3"></dot-title>
  <item-image-text wx:for="{{dataList2}}" obj="{{item}}" imgUrl="{{item.info_photos}}" itemTitle="{{item.info_content}}"
    itemInfo="{{item.info_summary}}" isImg="true" isDate="true" date="{{item.publishdate}}" isLine="true">
  </item-image-text>
  <!-- <view class="flex justify-between">
    <view wx:for="{{datalist2}}" wx:key="item">
      <view>
        <image src="{{baseImgUrl}}{{item_photos}}" style="height:276rpx;width:200rpx;"></image>
      </view>
      <view class="margin-tb-xs">{{item.info_content}}</view>
      <view class="text-sm text-grey">浏览 203 次</view>
    </view>
  </view> -->
</view>