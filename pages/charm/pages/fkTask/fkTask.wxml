<cu-custom bgColor="bg-gradual-blue" isBack="{{true}}">
  <view slot="content">任务反馈</view>
</cu-custom>
<view class="padding-lg">

  <view class="cu-form-group">
		<view class="title">任务名称：</view>
		<input placeholder="请输入任务名称" bindinput="putData" data-key="taskName" disabled></input>
	</view>

	
  <view class="cu-form-group">
		<view class="title">任务完成率：</view>
		<input type="number" placeholder="请输入任务完成率" bindinput="putData" data-key="taskName"></input>%
	</view>

	<view class="cu-form-group">
		<view class="title">任务反馈时间</view>
		<picker mode="date" value="{{date}}" start="{{endDate}}" end="2220-01-01" bindchange="dateChange">
			<view class="picker">
				{{endDate}}
			</view>
		</picker>
  </view>

	<view class="cu-form-group align-start">
		<view class="title">任务完成情况：</view>
		<textarea maxlength="-1"  placeholder="请输入任务内容" bindinput="putData" data-key="taskInfo"></textarea>
	</view>

	<!-- 图片上传 -->
	<view class="cu-bar bg-white">
		<view class="action">
			图片上传
		</view>
		<view class="action">
			{{imgList.length}}/9
		</view>
	</view>
	<view class="cu-form-group">
		<view class="grid col-3 grid-square flex-sub">
			<view class="bg-img" wx:for="{{imgList}}" wx:key="{{index}}" bindtap="ViewImage" data-url="{{imgList[index]}}">
				<image src='{{imgList[index]}}' mode='aspectFill'></image>
				<view class="cu-tag bg-red" catchtap="DelImg" data-index="{{index}}">
					<text class="cuIcon-close"></text>
				</view>
			</view>
			<view class="solids" bindtap="ChooseImage" wx:if="{{imgList.length<9}}">
				<text class="cuIcon-cameraadd"></text>
			</view>
		</view>
	</view>

	<!-- 提交 -->
	<view class="padding flex flex-direction">
		<button bindtap="commitData" class="cu-btn bg-green margin-tb-sm lg">提交反馈</button>
	</view>
</view>