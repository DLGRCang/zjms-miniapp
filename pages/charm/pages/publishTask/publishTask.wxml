<cu-custom bgColor="bg-gradual-blue" isBack="{{true}}">
  <view slot="content">发布任务</view>
</cu-custom>
<view class="padding-lr-lg">
  <!-- <dot-title title="任务列表" color="color"></dot-title> -->
  <scroll-view scroll-x class="bg-white nav">
    <view class="flex text-center">
      <view class="cu-item flex-sub {{index==TabCur?'color cur':''}}" wx:for="{{status}}" wx:key bindtap="tabSelect"
        data-id="{{index}}">
       {{item}}
      </view>
    </view>
  </scroll-view>

  <view class="flex justify-between margin-tb-lg" wx:if="{{TabCur==0}}" wx:for="{{5}}">
    <view class="">
      <view class="text-xl text-bold">准备发布会</view>
      <view class="margin-tb-xs text-grey">布置会场，布置会场布置会场布置会场布置会场布置会场布置会场</view>
    </view>
    <view class="fkbutton">
      <button class="cu-btn round bg-green shadow" bindtap="taskInfo">任务进度</button>
    </view>
  </view>
  <view class="flex justify-between margin-tb-lg" wx:if="{{TabCur==1}}" wx:for="{{5}}">
    <view class="">
      <view class="text-xl text-bold text-blue">准备发布会</view>
      <view class="margin-tb-xs text-grey">布置会场，布置会场布置会场布置会场布置会场布置会场布置会场</view>
    </view>
    <view class="fkbutton">
      <button class="cu-btn round bg-green shadow" bindtap="taskInfo">任务进度</button>
    </view>
  </view>
  <view class="flex justify-between margin-tb-lg" wx:if="{{TabCur==2}}" wx:for="{{5}}">
    <view class="">
      <view class="text-xl text-bold text-red">准备发布会</view>
      <view class="margin-tb-xs text-grey">布置会场，布置会场布置会场布置会场布置会场布置会场布置会场</view>
    </view>
    <view class="fkbutton">
      <button class="cu-btn round bg-green shadow" bindtap="taskInfo">任务进度</button>
    </view>
  </view>
  <view class="flex justify-between margin-tb-lg" wx:if="{{TabCur==3}}" wx:for="{{5}}">
    <view class="">
      <view class="text-xl text-bold text-yellow">准备发布会</view>
      <view class="margin-tb-xs text-grey">布置会场，布置会场布置会场布置会场布置会场布置会场布置会场</view>
    </view>
    <view class="fkbutton">
      <button class="cu-btn round bg-green shadow" bindtap="taskInfo">任务进度</button>
    </view>
  </view>



  <view>
    <button class="bg-green lg" bindtap="publishTaskForm">发布任务</button>
  </view>

  <view class="margin-top-lg">
    <button class="bg-green lg" bindtap="goVideo">视频会议</button>
  </view>

</view>